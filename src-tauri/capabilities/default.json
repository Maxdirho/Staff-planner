{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permessi base per la finestra principale di Staff Planner",
  "windows": ["main"],
  "permissions": ["core:default"]
}
